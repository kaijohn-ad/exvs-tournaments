# Cloudflare Workers configuration for Boost Bracket

name = "exvs-tournaments"
compatibility_date = "2024-10-01"

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "exvs-tournaments-db"
database_id = "preview_database_id"  # Replace with actual database ID after creation

# Environment variables (set via Cloudflare dashboard or wrangler secret)
# BASIC_AUTH_USER
# BASIC_AUTH_PASS
